#pragma once

// Энергосбережение: при простое усыпляем устройство (light sleep), просыпаемся
// по кнопке / подъёму руки / тапу. Состояние программы при этом сохраняется.
void powerBegin();         // инициализация энергосбережения
void powerNoteActivity();  // отметить активность (сбросить таймер простоя)
void powerTick();          // вызывать в главном цикле: при простое — полный сон (если не keepAwake)
void powerSleepNow();      // немедленно уйти в сон (по запросу: кнопка на главном экране)
