#pragma once

// Верхняя строка состояния (общая для всех экранов): время MSK слева, заряд справа.
void statusbarDraw();

// Поминутное обновление времени в статусбаре. Зовётся из главного цикла каждый
// кадр; сам троттлит (раз в секунду) и перерисовывает только при смене минуты,
// и только если текущая программа действительно показывает статусбар.
void statusbarTick();
